{
  "name": "@cloudwalk/chainshot",
  "version": "0.0.3",
  "description": "",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "prepare": "rm -rf dist && npm run build && cp -r src/templates dist/templates"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@cloudwalk/eslint-config-smart-contracts": "^1.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "typescript": "^5.9.2"
  },
  "peerDependencies": {
    "@nomicfoundation/hardhat-toolbox": ">=5.0.0",
    "@nomicfoundation/hardhat-ethers": ">=3.0.0"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "mocha-chai-jest-snapshot": "^1.1.7"
  }
}
